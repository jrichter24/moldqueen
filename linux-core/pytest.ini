[pytest]
# reference/ holds hardware scratch tools (e.g. mk4_test.py, which matches the
# *_test.py glob and runs argparse at import) - not unit tests. Don't collect there.
addopts = --ignore=reference
