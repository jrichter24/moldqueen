# Gradle
.gradle/
build/

# Local machine SDK pointer
local.properties

# IDE
*.iml
.idea/
captures/
.cxx/

# local monotonic build-number counter (baked into the APK; not tracked to avoid churn)
app/buildnum.properties
