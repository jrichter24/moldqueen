/*
 * Copy this file to  wifi_secrets.h  (same dir) and fill in your network.
 *   cp wifi_secrets.h.example wifi_secrets.h
 *
 * wifi_secrets.h is GITIGNORED — never commit real credentials. This is the simple
 * stepping stone; NVS-stored creds + fallback-AP provisioning are the WORKBOARD future.
 */
#ifndef WIFI_SECRETS_H
#define WIFI_SECRETS_H

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASS "your-wifi-password"

#endif /* WIFI_SECRETS_H */
