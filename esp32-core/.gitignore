# Defensive: WiFi creds now live in NVS at RUNTIME (provisioning — see mk4_provision),
# NOT compiled into any header. This guards against anyone re-introducing a creds file.
wifi_secrets.h

# ESP-IDF build output + generated config (never commit these)
/build/
/build_host_test
/build_host_test.exe
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
